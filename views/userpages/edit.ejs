<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Login to Overhyped</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link rel="stylesheet" href="/stylesheets/main.css">
    </head>
    
    <nav class="navbar navbar-inverse navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                      <span class="sr-only">Toggle navigation</span>
                      <span class="icon-bar"></span>
                      <span class="icon-bar"></span>
                      <span class="icon-bar"></span>
                    </button>

                    <a class="navbar-brand" href="/">OverHyped</a>
                </div>
                <div id="navbar" class="collapse navbar-collapse">
                  <ul class="nav navbar-nav">
                    <li class="<%= typeof page !== 'undefined' && page === 'userpages' ? 'active' : '' %>"><a href="/userpages">Home</a></li>
                  </ul>
                  <ul class="nav navbar-nav navbar-right">
                      <% if(!currentUser){ %> 
                      <li><a href="/login">Login</a></li>
                      <li><a href="/register">Sign Up</a></li>
                    <% } else { %>
                      <li><a href="">Signed In As <%= currentUser.username %></a></li>
                      <li><a href="/logout">Logout</a></li>
                    <% } %>
                  </ul>
                </div>
            </div>
        </nav>
    

<div class="container loginSignBorder" style="margin-top: 70px">


    <div class="row">
        <h1 style="text-align: center">Edit Your Page</h1>
            <div class="loginSign">
                <form action="/userpages/<%= userpage._id %>/?_method=PUT" method="POST">
                    <div class="form-group">
                        <label for="name">Page Name</label>
                        <input class="form-control" type="text" name="userpage[name]" value="<%=userpage.name%>" >
                    </div>
                    <div class="form-group">
                        <label for="image">Profile Picture</label>
                        <input class="form-control" type="text" name="userpage[image]" value="<%=userpage.image%>" >
                    </div>
                    <div class="form-group">
                        <label for="description">Short Bio</label>
                        <input class="form-control" type="text" name="userpage[description]" value="<%=userpage.description%>" >
                    </div>
                    <div class="form-group">
                      <button class="btn btn-lg btn-default btn-block btn-primary">Submit Changes</button>
                    </div>
                    
                </form>
                <a href="/userpages/<%= userpage._id%>">Go Back</a>

            </div>
    </div>

<% include ../partials/footer %>